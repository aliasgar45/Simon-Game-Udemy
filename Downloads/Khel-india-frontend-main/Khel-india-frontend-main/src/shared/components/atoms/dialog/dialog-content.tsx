import { DialogContent, DialogContentProps } from '@mui/material';

const StyledDialogContent = (props: DialogContentProps) => {
  return <DialogContent {...props} />;
};

export default StyledDialogContent;
